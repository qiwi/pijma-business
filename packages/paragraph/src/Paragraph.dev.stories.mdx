import { Paragraph } from './Paragraph'
import { Meta, Canvas, Story } from '@storybook/addon-docs'

<Meta
  title="Playground/Типографика"
  component={Paragraph}
  args={{
    size: '10',
    color: 'black',
  }}
  parameters={{
    viewMode: 'story',
    controls: {
      include: ['size', 'color'],
    },
  }}
/>

export const Template = (props) => (
  <Paragraph {...props}>Параграф {props.size}</Paragraph>
)

<Canvas>
  <Story name="Параграф">{Template.bind({})}</Story>
</Canvas>
